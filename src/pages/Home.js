import { Button, Typography } from '@mui/material'

function Home () {
  return (
    <div>
      <Typography variant='h4'>Netflix Clone</Typography>
      <Button variant='contained'>Button</Button>
    </div>
  )
}

export default Home
